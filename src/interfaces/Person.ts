interface Person {
    id: number;
    username: string;
    description: string;
}

interface PersonEvent {
    id: number;
    username: string;
    description: string;
    cancelled: boolean;
}